# 일본어 단어 장기기억

JLPT 단어를 간격 반복으로 외우는 개인 학습 앱. 안드로이드에서 앱으로 설치된다.

단어 데이터는 들어 있지 않다. 앱을 연 뒤 `＋ 단어 파일 업로드` 로 자기 JSON 파일을
넣어서 쓴다. 넣은 데이터와 학습 기록은 그 기기의 브라우저에만 저장된다.

## 설치

안드로이드 Chrome으로 접속 → 메뉴 → `앱 설치` (또는 `홈 화면에 추가`)
